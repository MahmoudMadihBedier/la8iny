import 'package:flutter/material.dart';

/// Navy blue used for headings and badge pin outline.
const Color rateUsNavy = Color(0xFF14213D);

/// Primary CTA blue.
const Color rateUsBlue = Color(0xFF1E88F0);

/// Yellow accent for badge pin fill.
const Color rateUsYellow = Color(0xFFFFC531);

/// Star rating golden color.
const Color rateUsStar = Color(0xFFFFB800);

/// Border color for the rating badge box.
const Color rateUsBorder = Color(0xFFECECEC);

/// Light border / dotted background color.
const Color rateUsLightBorder = Color(0xFFEDEDED);

/// Review card background color.
const Color rateUsReviewCardBg = Color(0xFFF5F5F7);
